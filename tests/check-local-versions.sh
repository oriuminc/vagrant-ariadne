#!/usr/bin
# @TODO: Check all versions and output a table
#   to compare recommended to actual.
sw_vers -productVersion
xcodebuild -version
gcc --version
rvm --version
ruby --version
bundle --version
vagrant --version
VBoxManage --version
